import 'package:flutter/material.dart';

class AllPostPage extends StatelessWidget {
  const AllPostPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("みんなの投稿")),
    );
  }
}
